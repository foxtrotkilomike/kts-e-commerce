const enum QueryParams {
  TITLE = "title",
  PRICE = "price",
  PRICE_MIN = "price_min",
  PRICE_MAX = "price_max",
  CATEGORY_ID = "categoryId",
  OFFSET = "offset",
  LIMIT = "limit",
}

export default QueryParams;
